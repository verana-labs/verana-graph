{{- if .Values.service.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "verana-graph.fullname" . }}
  labels:
    {{- include "verana-graph.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "verana-graph.selectorLabels" . | nindent 4 }}
{{- end }}
